# XmaxSDK

`ai.xmax.sdk` is the shared RTC package used by Xmax Unity applications.

The package currently contains only the initial scaffold. It has no dependency on a PICO SDK or an RTC provider yet.
